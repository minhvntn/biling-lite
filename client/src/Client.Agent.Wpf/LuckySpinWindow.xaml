<Window x:Class="Client.Agent.Wpf.LuckySpinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vòng quay may mắn"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Width="640"
        Height="780">
    <Window.Resources>
        <LinearGradientBrush x:Key="PurpleGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#8B5CF6" Offset="0"/>
            <GradientStop Color="#7C3AED" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="PurpleButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PurpleGradient}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#E2E8F0"/>
                                <Setter Property="Foreground" Value="#94A3B8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OutlineButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#FDFBFF" CornerRadius="12" BorderThickness="1" BorderBrush="#E2E8F0">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Color="Black" Opacity="0.15"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border BorderThickness="0,0,0,1" BorderBrush="#F1F5F9">
                <Grid Margin="20,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🎰" Foreground="{StaticResource PurpleGradient}" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Vòng quay may mắn" FontSize="18" FontWeight="SemiBold" Foreground="#1E293B" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Width="30" Height="30" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="CloseButton_Click">
                        <TextBlock Text="✕" FontSize="16" Foreground="#64748B" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title -->
                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,10,0,16">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
                        <TextBlock Text="✨" Foreground="#C084FC" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Text="THỬ VẬN MAY" FontSize="32" FontWeight="Black" Foreground="{StaticResource PurpleGradient}" VerticalAlignment="Center"/>
                        <TextBlock Text="✨" Foreground="#C084FC" FontSize="24" Margin="12,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="Mỗi lượt quay tốn 5 điểm" FontSize="15" Foreground="#64748B" HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Info Cards -->
                <Grid Grid.Row="1" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Available Points Card -->
                    <Border Grid.Column="0" Background="White" BorderBrush="#F1F5F9" BorderThickness="1" CornerRadius="12" Padding="20,16">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="Black" Opacity="0.05"/>
                        </Border.Effect>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Border Width="48" Height="48" CornerRadius="24" Background="#F3E8FF" Margin="0,0,16,0">
                                <TextBlock Text="⭐" FontSize="24" Foreground="#8B5CF6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Điểm hiện có" FontSize="13" Foreground="#64748B" Margin="0,0,0,2"/>
                                <TextBlock x:Name="AvailablePointsText" Text="0" FontSize="22" FontWeight="Bold" Foreground="#6D28D9"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Cost Card -->
                    <Border Grid.Column="2" Background="White" BorderBrush="#F1F5F9" BorderThickness="1" CornerRadius="12" Padding="20,16">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="Black" Opacity="0.05"/>
                        </Border.Effect>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Border Width="48" Height="48" CornerRadius="24" Background="#F1F5F9" Margin="0,0,16,0">
                                <TextBlock Text="🪙" FontSize="24" Foreground="#475569" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Chi phí" FontSize="13" Foreground="#64748B" Margin="0,0,0,2"/>
                                <TextBlock Text="5 điểm / lượt" FontSize="18" FontWeight="SemiBold" Foreground="#334155"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Wheel Area -->
                <Border Grid.Row="2" Background="White" BorderBrush="#F1F5F9" BorderThickness="1" CornerRadius="16" Padding="16,32,16,32" Margin="0,0,0,16">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="Black" Opacity="0.05"/>
                    </Border.Effect>
                    <Grid x:Name="WheelHost" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <!-- Code behind will inject the wheel here -->
                    </Grid>
                </Border>

                <!-- Status Panel -->
                <Border Grid.Row="3" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="16,12" Margin="0,0,0,16">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="ℹ️" Foreground="#8B5CF6" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="StatusText" Text="Nhấn QUAY NGAY để bắt đầu." FontSize="15" Foreground="#475569" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Actions -->
                <Grid Grid.Row="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="140"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button x:Name="SpinButton" Grid.Column="0" Content="QUAY NGAY!" Style="{StaticResource PurpleButtonStyle}" Height="56" Click="SpinButton_Click">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🔄" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                    
                    <Button x:Name="CloseActionButton" Grid.Column="2" Content="Đóng" Style="{StaticResource OutlineButtonStyle}" Height="56" Click="CloseButton_Click"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
