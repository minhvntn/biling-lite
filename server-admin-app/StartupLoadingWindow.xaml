<Window x:Class="Server.Admin.App.StartupLoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đang khởi động Server Admin"
        Width="460"
        Height="220"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#F8FAFC">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Server Admin đang khởi động"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#1E3A8A" />
        <TextBlock Grid.Row="1"
                   Margin="0,8,0,0"
                   Text="Vui lòng chờ, hệ thống đang tải dữ liệu..."
                   FontSize="13"
                   Foreground="#475569" />

        <ProgressBar Grid.Row="3"
                     x:Name="StartupProgressBar"
                     Height="18"
                     Minimum="0"
                     Maximum="100"
                     Value="10"
                     Foreground="#2563EB"
                     Background="#E2E8F0" />

        <TextBlock Grid.Row="5"
                   x:Name="StartupStatusTextBlock"
                   Text="Đang khởi tạo giao diện..."
                   FontSize="13"
                   FontWeight="SemiBold"
                   Foreground="#1E293B" />
    </Grid>
</Window>
